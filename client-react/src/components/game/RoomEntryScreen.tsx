// Placeholder — full RoomEntryScreen migration is a future batch. Renders a
// minimal shell so RoomScreen can mount when not yet in a room.
export default function RoomEntryScreen() {
  return (
    <div className="room-entry-stub p-4 text-center">
      <h2 className="text-gold">Citadels Online</h2>
      <p className="text-muted">RoomEntryScreen — to be migrated</p>
    </div>
  );
}
