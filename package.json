{
    "name": "citadels-online",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "build": "npm run build --prefix common && npm run build --prefix client-react && npm run build --prefix server",
        "lint": "npm run lint --prefix common && npm run lint --prefix server && npm run lint --prefix client-react",
        "typecheck": "npm run build --prefix common && npm run typecheck --prefix server"
    },
    "engines": {
        "node": "^20.0.0",
        "npm": ">=10.0.0"
    }
}
